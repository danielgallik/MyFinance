@{
    ViewData["Title"] = "Import Transactions";
}

<h2>Import Transactions</h2>

@if (TempData["ImportResult"] != null)
{
    <div class="alert alert-success">@TempData["ImportResult"]</div>
}

<form asp-action="ImportTransactions" method="post">
    <button type="submit" class="btn btn-primary">Import from Bank API</button>
</form>